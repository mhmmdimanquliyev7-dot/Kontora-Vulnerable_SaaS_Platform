# models

Prisma (in `/db`) is the source of truth for persisted data models and owns
the database schema/migrations. This folder holds API-layer concerns that sit
on top of those models: request/response DTOs, view models, and mapping
functions between Prisma entities and what the API exposes.

Empty for now — populated as features are added.
