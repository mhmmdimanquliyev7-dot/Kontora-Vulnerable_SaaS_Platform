# Used by Prisma CLI (migrate, studio, generate) when run from /db directly.
# In docker-compose, the api service gets DATABASE_URL injected via environment instead.
DATABASE_URL="postgresql://kontora:kontora_dev_password@localhost:5432/kontora?schema=public"
