# Used by Prisma CLI (migrate, studio, generate) when run from /db directly.
# In docker-compose, the api service gets DATABASE_URL injected via environment instead.
# Port 5433 matches docker-compose.yml's POSTGRES_PORT mapping (see root .env.example).
DATABASE_URL="postgresql://kontora:kontora_dev_password@localhost:5433/kontora"
