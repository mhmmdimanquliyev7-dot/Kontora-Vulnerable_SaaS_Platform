server:
  port: 8080
  tomcat:
    threads:
      max: 20
      min-spare: 2
    accept-count: 20
    max-connections: 200

spring:
  application:
    name: export-worker
  mvc:
    problemdetails:
      enabled: true

kontora:
  internal-api-key: ${INTERNAL_API_KEY:}

logging:
  level:
    root: INFO
